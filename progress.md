Original prompt: 1. 장신구 레벨업 시 선형적으로 밸런스 변경 (30% -> 36% 이런 느낌보다는10% -> 20% 으로 변경 요청)
2. 적이 죽었을 때 효과음 조정 (적의 수가 많아지니까 빈도 수가 너무 과함)
3. ESC 모달에서 사운드 조절할 수 있게 변경
4. ESC 모달에서 무기/장신구의 툴팁이 표시되지 않는 버그 수정 요청
5. ESC 모달에서 스크롤이 작동하지 않는 버그 수정 요청
6. ESC 모달에서 스탯에 골드 관련도 추가
7. 투사체 지속시간 관련된 장신구 및 상점에 추가
8. 레벨업 시 보상 선택에서 텍스트 "효과 강화" -> "~~ 증가 10%"이런 식으로 상세하게 변경 요청
9. ESC 모달에서 전투 포기 버튼 추가 및 갱신 코드 추가
10. 보스 5분마다 등장하게 변경 요청 (6번째 보스 격파 시 게임 승리)

- 2026-03-21: 장신구 선형 밸런싱, 투사체 지속시간 장신구/영구 업그레이드, 상세 업그레이드 설명, ESC 오디오 패널/전투 포기/골드 스탯, 적 사망 SFX throttle, 5분 주기 보스 및 6보스 승리 흐름 구현.
- 2026-03-21: `tests/DeathSystem.test.js`를 실제 `bossData` 기반으로 정리해 보스 개수 하드코딩 의존을 제거.
- 2026-03-21: `node scripts/validateData.js`, `node scripts/runTests.js`, `npm run build` 재검증 통과.
- 2026-03-21: 로컬 dev/serve 서버는 세션 내부에서는 기동되지만 현재 실행 환경의 포트 격리 때문에 별도 프로세스/브라우저에서 접속 검증을 재현하지 못함.
- 2026-03-21: ESC 모달 폭 확대, 카드 본문 1줄 요약화, 메인메뉴 버튼 제거, 스탯에 투사체 크기/범위·지속시간 추가, 백틱 디버그 패널 제거.
- 2026-03-21: `tests/PauseAndInputSource.test.js` 추가 후 red-green으로 회귀 검증. 전체 테스트 23개 및 빌드 재통과.
- 2026-03-21: `CodexView` 스크롤 불가 원인을 `#ui-container`의 `pointer-events:none` 상속으로 확인. `.cx-root`에 `pointer-events:auto`를 추가하고 `tests/TitleAndCodexSource.test.js`에 회귀 테스트를 추가. 전체 테스트와 빌드 재통과.
- 2026-03-21: `CodexView` 무기 도감의 진화 무기 잠금 판정이 `weaponsUsedAll`만 보던 문제를 수정. `evolvedWeapons`도 별도로 읽어 진화 무기 카드를 해금 상태로 렌더링하고 발견 수 카운트에도 반영. 전체 테스트와 빌드 재통과.
- 2026-03-21: 레벨업 리롤/봉인 1차 구현 완료. `reroll_charge`/`banish_charge` 메타 업그레이드, `runRerollsRemaining`/`runBanishesRemaining`/`banishedUpgradeIds` 월드 상태, `stat_gold` fallback, `UpgradeApplySystem`의 `currencyEarned` 이벤트 발행, `LevelSystem`의 봉인 후보 제외를 추가.
- 2026-03-21: `LevelUpView`를 설정 객체 기반으로 확장해 `남은 리롤`, `남은 봉인`, `봉인 모드`, 카드별 `리롤` 버튼을 렌더링하도록 변경. `PlayScene`에 카드 선택/카드별 리롤/봉인 모드 토글/카드 봉인 핸들러를 추가하고 `UpgradeSystem.replaceChoiceAtIndex()`는 현재 슬롯 id까지 제외하도록 수정.
- 2026-03-21: 신규 테스트 `tests/UpgradeApplySystem.test.js`, `tests/LevelUpSource.test.js`, `tests/WorldState.test.js` 추가. `node scripts/validateData.js`, `node scripts/runTests.js`, `npm run build` 모두 통과.
- 2026-03-21: Playwright로 `http://127.0.0.1:4176/`에서 타이틀 → 시작 무기 선택 → 게임 진입까지 smoke 검증. `.playwright-cli/page-2026-03-21T09-50-39-414Z.png` 기준 HUD/전장 렌더 정상. 다만 레벨업 오버레이는 결정적으로 재현할 수 있는 디버그 훅이 없어 브라우저에서 실동작까지는 아직 미확인.
- 2026-03-21: Playwright smoke 중 타이틀 하단에 `디버그: \`` 안내가 남아 있는 것을 발견해 `TitleScene`에서도 디버그 힌트를 제거. 전체 테스트/빌드 재통과.
- 2026-03-21: 남은 UI/콘텐츠 확장 반영. `PauseView` 무기 카드는 이름/아이콘/태그/레벨만 남기도록 간소화했고, 장신구 카드는 `현재 레벨` 설명만 노출하도록 변경. `ResultView`의 강화 상점 버튼은 제거하고 `메인 화면으로` 버튼으로 교체해 `TitleScene` 복귀로 연결.
- 2026-03-21: `enemyData`에 보스 6종(`boss_lich`, `boss_warden`, `boss_broodmother`, `boss_titan`, `boss_seraph`, `boss_abyss_eye`) 추가, `bossData`를 5분 간격 6종 순차 등장 구조로 교체. `waveData`는 12개 구간으로 세분화해 후반 `spawnPerSecond`와 `eliteChance`를 크게 증가시킴.
- 2026-03-21: `unlockData`를 8개 보상으로 확장. `chain_lightning`, `coin_pendant`, `wind_crystal`, `greed_amulet` 해금 조건 추가. `PlayResultHandler`/`UnlockEvaluator` 회귀 테스트도 함께 갱신.
- 2026-03-21: 신규 테스트 `tests/ResultAndProgressionSource.test.js` 추가. `node scripts/validateData.js`, `node scripts/runTests.js`, `npm run build` 모두 통과.
- 2026-03-21: `subscreenTheme`에 공통 `renderSubscreenHeader()` / `renderSubscreenFooter()` helper를 추가하고 `CodexView`/`SettingsView`/`MetaShopView`가 공통 서브스크린 셸 마크업을 직접 재사용하도록 정리. `sceneNavigation`에는 `change()` / `load()`를 추가해 `TitleScene`의 shop 포함 모든 서브화면 전환을 같은 가드 경계로 통일.
- 2026-03-21: Playwright로 `http://127.0.0.1:4177/`에서 `Meta Shop`/`Codex`/`Settings` 진입과 `Escape` 복귀를 smoke 확인. 모바일 뷰포트(390x844)에서 `Meta Shop`의 `.ms-root` 스크롤 높이 `2407px`, wheel 입력 후 `scrollTop`이 `0 -> 500`으로 변해 실제 스크롤 동작도 확인. 콘솔 에러/경고 없음.
- 2026-03-21: 신규 helper/source 회귀 테스트(`tests/SubscreenTheme.test.js`, `tests/SceneNavigation.test.js`, `tests/SceneInfrastructureSource.test.js`, `tests/TitleAndCodexSource.test.js`) 갱신 후 `node scripts/runTests.js`, `npm run build` 재검증 통과.
- 2026-03-21: ESC 일시정지 모달을 `로드아웃` 단일 탭 + 선택형 상세 패널 구조로 재설계. `pauseLoadoutContent` helper로 장비/빈 슬롯/잠금 슬롯 리스트와 상세 패널을 렌더하고, `PauseView`는 선택 상태/탭/툴팁 wiring만 유지하도록 정리. 카드 하단 assist row(시너지/진화/진행), 실제 관계 기반 linked items, tooltip 보조화도 함께 반영.
- 2026-03-21: 신규 회귀 테스트 `tests/PauseLoadoutContent.test.js`, 갱신된 `tests/PauseAndInputSource.test.js`, `tests/ResultAndProgressionSource.test.js` 포함 전체 `scripts/runTests.js` 42 PASS, `npm run build` 통과. 이번 세션에서는 브라우저 smoke는 다시 돌리지 못했고, 실제 런타임 상호작용은 다음 Playwright 검증에서 한 번 더 확인 필요.
- 2026-03-21: Playwright로 실제 전투 중 레벨업 오버레이를 재현해 `남은 리롤 2`, `남은 봉인 1` 노출을 확인. 가운데 카드 리롤 후 `마법탄 + -> 골드 획득`으로 교체되고 `남은 리롤 2 -> 1` 감소 확인, 봉인 모드에서 `골드 획득` 카드 클릭 후 `남은 봉인 1 -> 0` 감소와 카드 교체도 확인.
- 2026-03-21: Playwright 실제 입력으로 레벨업 종료 후 전투 중 `Escape` 장입 상태(`keyboard.down`)를 유지해 ESC 일시정지 모달 표시를 확인. `.pv-overlay` DOM 상태가 `display:flex`, `aria-hidden=false`로 바뀌고 무기/장신구/스탯/사운드 탭, `전투 포기`, `재개` 버튼 렌더도 함께 확인. 짧은 `press Escape`만으로는 poll 기반 입력 프레임과 어긋나 검증이 흔들릴 수 있음.
- 2026-03-21: Playwright로 ESC 모달 추가 실검증 완료. 무기 카드 hover 시 `.pv-tooltip`이 `display:block`으로 열리고 상세 스탯/시너지/진화 조건 텍스트가 렌더됨. `사운드` 탭에서 `masterVolume 80 -> 72`, `soundEnabled ON -> OFF`를 변경했고 `ashenRequiem_session.options`에도 같은 값이 저장되는 것을 확인. 모바일 축소(390x600)에서는 `.pv-panel`의 `scrollHeight 739`, `clientHeight 558`, wheel 입력 후 `scrollTop 0 -> 181`로 실제 내부 스크롤 확인. `전투 포기` 클릭 후 결과 화면 `☠ GAME OVER`와 `⌂ 메인 화면으로`/`다시 시작` 버튼 노출도 확인.
- 2026-03-21: 장신구 툴팁 브라우저 최종 검증 완료. Playwright에서 실제 `PlayScene` 진입 후 `tome_of_power` 장신구를 주입하고 ESC pause overlay를 띄운 다음 장신구 카드 hover로 `.pv-tooltip`이 `visible=true` 상태에서 `마력의 서`, `희귀`, `데미지 Lv1 +10% → Lv2 +20% → Lv3 +30% → Lv4 +40% → Lv5 +50%`, `마법탄 진화 연결` 텍스트를 렌더하는 것을 확인. 최종 스크린샷은 `output/playwright/pause-accessory-tooltip.png`.
- 2026-03-21: 구조 정리 추가 반영. `PauseView`는 `src/ui/pause/pauseViewSections.js`, `src/ui/pause/pauseTooltipContent.js`로 렌더/tooltip 빌더를 분리했고, `ResultView`/`PauseView` 액션 버튼은 `src/ui/shared/actionButtonTheme.js` 공통 토큰을 사용하도록 변경. `PlayScene`의 레벨업 선택/리롤/봉인 로직은 `src/scenes/play/levelUpController.js`로 이동.
- 2026-03-21: `src/core/runtimeHooks.js`를 추가하고 `Game`에서 `window.advanceTime(ms)` / `window.render_game_to_text()`를 등록하도록 변경. Playwright로 `http://127.0.0.1:4177/`에서 훅 존재를 확인했고, 실제 플레이 진입 후 `elapsedTime`이 `1.8474 -> 2.8474`로 증가해 `advanceTime(1000)` 반영도 확인.
- 2026-03-21: 새 source 테스트 `tests/UiStructureSource.test.js` 추가, `tests/LevelUpSource.test.js`를 controller 위임 구조에 맞게 갱신. `node scripts/runTests.js` 39개 PASS, `npm run build` 통과.
- 2026-03-21: 브라우저 검증 훅 후속 보강. `render_game_to_text()` snapshot에 `ui.pauseVisible`, `ui.levelUpVisible`, `ui.resultVisible`를 추가해 ESC/레벨업/결과 화면 smoke를 DOM 파싱 없이도 판별할 수 있게 했고, `tests/RuntimeHooks.test.js`로 회귀를 고정.

- 2026-03-21: 타이틀 종료 버튼 활성화 작업 시작. 종료 버튼 source 회귀 테스트를 먼저 추가해 red 확인 예정.
- 2026-03-21: TitleScene Quit 버튼을 활성화. window.close() 시도 후 차단 시 라이브 메시지 폴백을 추가하고 비활성 스타일/문구를 제거.
- 2026-03-21: 종료 버튼 변경 후 source 회귀 테스트와 전체 테스트(39/39), 빌드 통과 확인. 정적 서버 기반 Playwright smoke는 현재 소스와 다른 타이틀 UI가 렌더되어 Quit 클릭 검증 근거로 사용하지 않음.
- 2026-03-21: 레벨업 리롤 버튼을 카드 내부에서 카드 하단 외부로 옮기는 작업 시작. source 테스트를 먼저 갱신해 red 확인 예정.
- 2026-03-21: LevelUpView를 카드 래퍼 + 하단 풋터 액션 구조로 변경. 리롤 버튼을 카드 외부 아래로 이동.
- 2026-03-21: PauseView 시너지 보너스 포맷터 누락 버그 수정 시작. source 회귀 테스트를 먼저 추가해 red 확인 예정.
- 2026-03-21: pauseTooltipContent의 formatWeaponSynergyBonus를 export하고 PauseView가 재사용하도록 수정. 정의되지 않은 _formatSynergyBonus 호출 제거.
- 2026-03-21: 무기 progression/Lv.7 테스트를 새 구조 기준으로 갱신. 개별 테스트 RED 확인 중.
- 2026-03-21: 신규 weapon behavior/무기 테스트 추가. 레지스트리, spawn config, projectile runtime 동작 RED 확인 예정.
- 2026-03-21: 무기 성장 구조를 `weaponProgressionData` 단일 소스로 전환. 무기 업그레이드 카드는 무기당 1종만 남기고, 레벨 2~7 성장표를 번갈이 적용하도록 변경. 기본/진화 무기 `maxLevel`과 진화 조건도 모두 Lv.7 기준으로 상향.
- 2026-03-21: 신규 기본 무기 6종(`solar_ray`, `piercing_spear`, `flame_zone`, `venom_bog`, `crystal_shard`, `radiant_orb`)과 신규 behavior 3종(`laserBeam`, `groundZone`, `ricochetProjectile`) 추가. `ProjectileSystem`에 장판 tick reset / 반사 재조준 처리와 draw registry도 함께 확장.
- 2026-03-21: 신규 무기 전용 진화 무기 6종(`helios_lance`, `astral_pike`, `inferno_field`, `plague_marsh`, `prism_volley`, `seraph_disc`) 및 진화 레시피를 추가. 신규 무기 밸런스는 역할 공백 메우기형 기준으로 기본 스펙과 progression 수치를 1차 조정.
- 2026-03-21: 브라우저 smoke 재검증은 `dist/`를 Python 정적 서버(`127.0.0.1:43210`)로 서빙해 진행. Playwright로 시작 무기 선택에서 `solar_ray`, `flame_zone`, `crystal_shard` 선택 후 실제 게임 진입 스크린샷을 확보했고, `output/web-game/start-three/summary.json`에서 각 시작 무기가 플레이어 보유 무기로 반영됨을 확인.
- 2026-03-21: Playwright로 Codex 무기 탭을 열어 신규 진화 무기 카드 ID 6종(`helios_lance`, `astral_pike`, `inferno_field`, `plague_marsh`, `prism_volley`, `seraph_disc`)이 DOM에 실제 존재함을 확인. 스크린샷은 `output/web-game/codex-shot/codex.png`, ID 목록은 `output/web-game/codex-shot/ids.json`.
- 2026-03-21: `runtimeHooks`가 `scene.constructor.name`에 의존해 minify된 `"e"`를 노출하던 문제를 수정. 모든 scene 클래스에 안정적인 `sceneId`를 추가하고 `tests/RuntimeHooks.test.js`로 red-green 회귀를 고정.
- 2026-03-21: `dist/`를 `127.0.0.1:43210`에서 다시 서빙해 `WEB_GAME_CLIENT` 및 Playwright smoke를 재실행. `output/web-game/runtimehooks-play-client2/state-0.json`에서 `scene: "PlayScene"` 복구를 확인했고, 스크린샷은 `output/web-game/runtimehooks-play-client2/shot-0.png`.
- 2026-03-21: 다음 기능 작업으로 deterministic browser smoke coverage를 선정. 실행 계획은 `docs/superpowers/plans/2026-03-21-deterministic-browser-smoke-coverage.md`에 기록.
- 2026-03-21: deterministic browser smoke runner 초안 구현. `scripts/browser-smoke/runDeterministicSmoke.mjs`와 `scripts/browser-smoke/scenarios.js`를 추가하고 `tests/BrowserSmokeSource.test.js`로 source 계약을 고정.
- 2026-03-21: deterministic browser smoke를 `title_to_play`, `pause_overlay`, `result_screen` 3개 시나리오로 안정화. `render_game_to_text()` snapshot과 Playwright snapshot ref를 함께 사용해 title → play, pause overlay + 무기 tooltip hover, result overlay를 재현하고 `output/web-game/deterministic-smoke/` 아래에 summary/screenshot 산출물을 남김.
- 2026-03-21: fresh verification 완료. `tests/BrowserSmokeSource.test.js`, `tests/RuntimeHooks.test.js`, `scripts/browser-smoke/runDeterministicSmoke.mjs --url http://127.0.0.1:43210 --all`, `scripts/runTests.js --runInBand` 43 PASS, `npm run build` 통과.
- 2026-03-22: 다운로드 디자인안을 현재 pause 구조에 맞게 이식. `pauseLoadoutContent`를 슬롯형 무기/장신구 섹션 UI로 재구성하고, `PauseView` 선택/툴팁 바인딩과 injected CSS를 `.pv-slot-card` 기준으로 갱신. 잠금 슬롯 힌트는 유지했고 selectionKey는 전역 고유값으로 재생성.
- 2026-03-22: 회귀 테스트 `tests/PauseLoadoutContent.test.js`, `tests/PauseAndInputSource.test.js`, `tests/ResultAndProgressionSource.test.js`를 새 슬롯형 구조 기준으로 갱신. fresh `node scripts/runTests.js` 44 PASS, `npm run build` 통과.
- 2026-03-22: `scripts/browser-smoke/runDeterministicSmoke.mjs --url http://127.0.0.1:43210 --scenario pause_overlay` 검증 완료. pause overlay 열림, 무기 카드 hover 툴팁 표시, summary `output/web-game/deterministic-smoke/summary.json`에서 `allPassed: true` 확인.
- 2026-03-23: ESC pause 상세 패널에서 일부 텍스트가 검게 보이던 문제를 수정. 원인은 `pauseStyles`에서 `pv-loadout-power-row`, `pv-loadout-link-key/val`, `pv-loadout-synergy-state`, `pv-loadout-evolution-name/state` 색 지정 누락이었고, 대비 색을 명시적으로 추가.
- 2026-03-23: `tests/PauseAndInputSource.test.js`에 상세 패널 텍스트 대비 회귀 테스트 추가. 개별 pause source 테스트 통과, `npm run build` 통과, `scripts/browser-smoke/runDeterministicSmoke.mjs --url http://127.0.0.1:43210 --scenario pause_overlay` 재검증 통과.
- 2026-03-23: ESC 상세 패널 후속 개선 반영. 선택 카드와 상세 패널을 동일 accent/icon으로 연결하고, 상태/연결/시너지/진화 섹션 hierarchy를 분리했으며, status row label/value 고정 정렬, 빈 상태 안내 문구 구체화, 진화 결과 강조, 모바일 2줄 스택 대응까지 적용. 회귀 테스트(`tests/PauseLoadoutContent.test.js`, `tests/PauseAndInputSource.test.js`) 갱신 후 fresh `node scripts/runTests.js` 58 PASS, `npm run build` PASS, deterministic smoke `pause_overlay` `allPassed: true` 확인.
- 2026-03-23: ESC 상세 패널 2차 polish 반영. `pauseLoadoutContent`에 시너지 요구 조건/진화 결과를 실제 무기·장신구 icon 자산 기반 칩으로 렌더하는 helper를 추가하고, `normalizePauseSynergyRequirementId()`가 `acc_` prefix도 장신구 id로 풀도록 보강. `pauseStyles`에서는 synergy/evolution row 대비와 padding을 조정하고 meta icon, requirement chip, result icon 스타일을 추가. 회귀 테스트(`tests/PauseLoadoutContent.test.js`, `tests/PauseAndInputSource.test.js`) 갱신 후 fresh `node scripts/runTests.js` 58 PASS, `npm run build` PASS, deterministic smoke `pause_overlay` `allPassed: true` 확인.
- 2026-03-23: 타이틀 도감에 장신구 도감 탭 추가. `codexAccessoryTab` helper를 신설해 장신구 카드 렌더와 발견 gating을 분리했고, `CodexView`는 `장신구 도감` 탭/패널과 총 발견 수 집계(`적 + 무기 + 장신구`)를 반영. 도감용 세션 메타에 `accessoriesOwnedAll`을 추가하고 `accessoryAcquired` 이벤트를 통해 런 중 획득 장신구를 기록하도록 확장. 회귀 테스트(`tests/SessionMeta.test.js`, `tests/SessionState.test.js`, `tests/CodexViewHelpers.test.js`, `tests/TitleAndCodexSource.test.js`) 갱신 후 fresh `node scripts/runTests.js` 63 PASS, `npm run build` PASS.
- 2026-03-23: 우선순위 후속 구조 정리 반영. `PlayScene`은 `src/scenes/play/playSceneFlow.js`로 DPR 동기화, pause 토글, result overlay, pause 옵션 저장, frame 실행 흐름을 분리했고, `SettingsView`는 `settingsViewSections.js` / `settingsViewIcons.js` / `settingsViewStyles.js`로 분해. `LevelUpView`도 `src/ui/levelup/levelUpContent.js`로 헤더/카드 마크업을 위임.
- 2026-03-23: `CodexView` 기록 계산을 `src/ui/codex/codexRecords.js`로 분리해 발견 수, 업적, 해금 보상, 진화 무기 판정을 helper 계약으로 이동. `scripts/profile.js`는 `scripts/profileRuntime.js`를 통해 팩토리 시스템 로딩/헤드리스 컨텍스트 구성을 재사용하도록 정리.
- 2026-03-23: 남아 있던 brittle source 테스트를 helper/행동 기반으로 축소. 신규 테스트 `tests/SettingsViewHelpers.test.js`, `tests/PlaySceneFlowHelpers.test.js`, `tests/CiWorkflow.test.js` 추가, `tests/LevelUpSource.test.js`, `tests/ProfileSource.test.js`, `tests/SceneInfrastructureSource.test.js`, `tests/CentralizationSource.test.js`, `tests/TitleAndCodexSource.test.js`를 새 구조에 맞게 갱신.
- 2026-03-23: CI 기준선 추가. `.github/workflows/verify.yml`에서 Windows 기준 `npm test`, `npm run test:smoke`, `npm run build`를 실행하도록 설정했고, `package.json`의 `verify`도 build까지 포함하도록 확장.
- 2026-03-23: fresh verification 기준 `npm test` 53/53 PASS, `vite build` PASS. deterministic smoke는 `output/web-game/deterministic-smoke/summary.json`이 `allPassed: true`로 갱신됐고 stable baseline은 `title_to_play`, `pause_overlay`, `result_screen` 3개 시나리오만 `--all`에 포함하도록 조정.
- 2026-03-23: 도감 후속 polish 반영. `CodexView`에 분리 발견 통계 strip, 장신구 검색/희귀도/효과 필터, 장신구 상세 패널, 무기→장신구 점프 훅을 추가했고 `codexRecords`에는 희귀 장신구/진화 촉매 업적과 `buildCodexDiscoverySummary()`를 추가. deterministic smoke `title_codex`를 장신구 패널/필터/힌트 검증까지 확장했고 fresh `node scripts/runTests.js` 63 PASS, `npm run build` PASS, `node scripts/browser-smoke/runDeterministicSmoke.mjs --url http://127.0.0.1:43210 --scenario title_codex` `allPassed: true` 확인.
- 2026-03-23: 후속 구조 개선 반영. `PauseView`는 `pauseViewLifecycle.js` / `pauseAudioController.js` / `pauseTooltipBindings.js`로 show-hide 상태, 오디오 옵션 바인딩, tooltip binding 책임을 분리했다. browser smoke transport는 `smokeCliPaths.mjs` / `smokeCliRunner.mjs` / `smokeCliParsers.mjs` / `smokeSessionTransport.mjs`로 분해했고 `smokeCliTransport.mjs`는 facade re-export만 남겼다. Codex accessory 탭은 `codexAccessoryModel.js` / `codexAccessoryRender.js` / `codexAccessoryStyles.js`로 나누고 `codexStyles.js`에서 fragment를 조합하도록 정리했다.
- 2026-03-23: 구조 분리 후 `PauseView` tooltip helper 주입 typo(`showPauseTooltip`)로 deterministic smoke `pause_overlay`가 깨지는 것을 `npm run verify`에서 발견했다. `tests/PauseAndInputSource.test.js`에 explicit source regression을 추가한 뒤 `PauseView.js`에서 `showTooltip: showPauseTooltip`으로 수정해 red-green 확인.
- 2026-03-23: fresh verification 완료. `npm run typecheck` PASS, `npm test` 65/65 PASS, `npm run verify` PASS. deterministic smoke summary `output/web-game/deterministic-smoke/summary.json`는 `allPassed: true`.
- 2026-03-26: `http://localhost:8000` 정적 루트 서빙에서 `/favicon.svg` 404와 결과 오버레이 지연 import 실패를 점검. 원인으로 `PlayUI`의 lazy overlay loader가 fetch 시점을 런 후반까지 미루고, 한 번 실패한 rejected promise를 그대로 캐시하며 `showResult()`가 `Uncaught (in promise)`를 그대로 노출하는 점을 확인했다.
- 2026-03-26: `PlayUI`에 pause/result/level-up overlay 모듈 prewarm, module/view promise 분리, 실패 시 promise reset + `false` 반환 로직을 추가해 preload 실패 후 재시도와 late fetch 회복을 가능하게 했다. 정적 루트 서빙 호환을 위해 repo root에도 `favicon.svg`를 추가했다.
- 2026-03-26: 회귀 테스트 `tests/PlayUiLazyViews.test.js`, `tests/BrowserSmokeSource.test.js`를 red-green으로 확장했고 fresh `npm test` 88 PASS, `npm run build` PASS 확인. 로컬 `python3 -m http.server 8000` 기준 `GET /favicon.svg`와 `GET /src/ui/result/ResultView.js`가 모두 `200 OK`로 응답함을 확인했다.
- 2026-03-26: 아키텍처 후속 정리. `sessionFacade`가 compatibility shim `startLoadoutRuntime` 대신 실제 `domain/meta/loadout/startLoadoutDomain`을 직접 사용하도록 변경하고, `scripts/checkBoundaries.js`를 추가해 shim import, `domain -> ui/renderer/browser/scenes`, `scenes -> systems` 직접 의존을 검사하도록 했다. `package.json`의 `verify:fast`/`verify:ci`에 `check:boundaries`를 포함했다.
- 2026-03-26: deterministic smoke를 확장해 `title_meta_shop`, `title_settings_persist` 시나리오를 추가했다. 메타 상점 구매와 설정 저장을 각각 화면 상태 + `ashenRequiem_session` localStorage persistence까지 검증한다.
- 2026-03-26: world ownership migration 마무리. `CollisionSystem`, `SpawnSystem`, `EffectTickSystem`, `CameraSystem`, `EliteBehaviorSystem`, `StatusEffectSystem`이 top-level world alias 대신 `world.entities/run/runtime/queues/presentation`만 사용하도록 정리했고 관련 source/unit 테스트를 ownership shape로 갱신했다.
- 2026-03-26: result smoke 회귀 수정. `result_screen`은 더 이상 `GAME OVER`를 기대하지 않고 실제 UI 계약인 `DEFEAT/VICTORY`, restart 버튼, 메인 화면 버튼을 검증한다. `runtimeHooks.openResultOverlay()`도 debug overlay에서 restart/title callback을 넘겨 실제 결과 UI 구조를 재현하도록 수정했다.
- 2026-03-26: browser smoke 종료 경로 보강. `smokeSessionTransport`에 explicit `close()`를 추가하고 `runDeterministicSmoke`가 session transport close를 bounded cleanup으로 호출하도록 바꿨다. 긴 러닝으로 오해하던 full smoke는 실제 약 106초가 걸리며 정상 종료됨을 확인했다.
- 2026-03-26: fresh verification 완료. `npm run verify:ci`가 `typecheck`, `profile:check`, `check:boundaries`, 테스트 102/102, deterministic smoke `allPassed: true`, `vite build`까지 모두 통과했고 총 소요 시간은 약 113초였다.
- 2026-03-26: 남은 구조 정리 후속 반영. `playerSpawnRuntime`, `playSceneFlow`, `levelUpFlowRuntime`의 orchestration 로직을 각각 `src/app/play/playerSpawnApplicationService.js`, `src/app/play/playSceneFlowService.js`, `src/app/play/levelUpFlowService.js`로 물리 이동하고 기존 경로는 호환 re-export wrapper로 축소했다. `PlayScene`, `levelUpController`, `startRunApplicationService`, `profileRuntime`는 새 소유 모듈을 직접 사용하도록 갱신했다.
- 2026-03-26: smoke suite 최적화. `scripts/browser-smoke/scenarios.js`에 `suite` 메타를 추가해 core(`title_to_play`, `title_meta_shop`, `title_settings_persist`, `pause_overlay`, `result_screen`)와 extended(`title_codex`, `title_settings`)를 분리했고, `package.json`에 `test:smoke`(core)와 `test:smoke:full`(all) 스크립트를 나눴다. core smoke는 약 76초, full smoke는 약 119초로 측정됐다.
- 2026-03-26: boundary/tooling 정리. `checkBoundaries`가 새 wrapper 경로(`playerSpawnRuntime`, `playSceneFlow`, `levelUpFlowRuntime`)도 금지하도록 확장됐고, source guard 테스트를 새 app 소유 모듈 기준으로 갱신했다. README와 `docs/architecture-current.md`도 core/full smoke 구분을 반영했다.
- 2026-03-26: 최종 검증 재완료. fresh `npm run verify:ci`가 `typecheck`, `profile:check`, `check:boundaries`, 테스트 102/102, core smoke `allPassed: true`, `vite build`까지 모두 통과했고 총 소요 시간은 약 84초로 단축됐다.
- 2026-03-26: wrapper/운영 후속 마무리. `docs/compatibility-wrappers.md`를 추가해 남은 wrapper를 `keep-public-wrapper` / `remove-when-callers-migrate`로 분류했고, 특히 `playerSpawnRuntime`, `playSceneFlow`, `levelUpFlowRuntime`를 다음 삭제 우선순위로 명시했다.
- 2026-03-26: extended smoke를 별도 workflow로 승격. `.github/workflows/smoke-extended.yml`이 nightly schedule + `workflow_dispatch`로 `npm run test:smoke:full`을 실행하도록 추가됐다. 기본 `verify.yml`은 계속 `npm run verify:ci`만 사용한다.
- 2026-03-26: final fresh verification 완료. `npm run verify:ci` 재실행 결과 `typecheck`, `profile:check`, `check:boundaries`, 테스트 102/102, core smoke `allPassed: true`, `vite build`까지 모두 통과했고 총 소요 시간은 약 88초였다.
- 2026-03-26: 내부 호출자 migration 마감. `tests/`와 런타임 import를 `src/app/play/playerSpawnApplicationService.js`, `src/app/play/playSceneFlowService.js`, `src/app/play/levelUpFlowService.js`로 직접 연결하고, `playerSpawnRuntime`/`playSceneFlow`/`levelUpFlowRuntime`는 공개 호환 wrapper로만 남겼다. `checkBoundaries`는 이제 `src/`, `tests/`, `scripts/`에서 이 세 wrapper 재유입을 막는다.
- 2026-03-26: 아키텍처 lint baseline 추가. `package.json`에 `lint`, `lint:architecture`, `check:architecture-docs`를 추가했고 `verify:fast`/`verify:ci`는 이제 lint 단계를 공통 사용한다. `scripts/checkArchitectureDocs.mjs`는 `architecture-current.md` generated snapshot section, `compatibility-wrappers.md` shim inventory, README command drift를 검사한다.
- 2026-03-26: deterministic smoke 산출물 root를 `output/web-game/deterministic-smoke-core/`와 `output/web-game/deterministic-smoke-full/`로 분리했다. `runDeterministicSmoke`는 실행 모드에 따라 summary/artifact root를 다르게 기록한다.
- 2026-03-26: smoke 실행 주의점 확인. Windows/Vite 환경에서 `test:smoke`와 `test:smoke:full`을 병렬 실행하면 `dist/assets` 정리 충돌로 `EPERM`이 발생할 수 있어 smoke는 순차 실행이 안전하다.
- 2026-03-26: fresh 검증 재완료. `npm run test:smoke`는 `outputRoot=output/web-game/deterministic-smoke-core`, `npm run test:smoke:full`은 `outputRoot=output/web-game/deterministic-smoke-full`로 각각 `allPassed: true`를 기록했고, 이후 fresh `npm run verify:ci`도 `typecheck`, `profile:check`, `lint`, 테스트 102/102, core smoke, `vite build`까지 모두 통과했다.
- 2026-03-26: 편집 시점 아키텍처 가드 추가. `eslint.config.js`를 도입하고 `package.json`에 `lint:eslint`를 추가해 `no-restricted-imports` 기반으로 `domain -> presentation/browser`, `scene -> systems`, repo 내부 migrated wrapper import 금지를 editor/CLI lint 단계에서도 잡도록 했다. 기존 `check:boundaries`/`check:architecture-docs`는 계속 SSOT 검증으로 유지한다.
- 2026-03-26: smoke 중복 build 최적화. `runSmokeAgainstPreview.mjs`에 `--skip-build` 옵션과 주입 가능한 helper seam을 추가했고, `smoke:core:prebuilt`, `smoke:full:prebuilt` 스크립트를 만들었다. `verify:ci`는 이제 `build` 한 번 후 `smoke:core:prebuilt`를 실행해 core smoke 검증을 재사용한다.
- 2026-03-26: extended smoke workflow도 prebuilt 경로로 전환. `.github/workflows/smoke-extended.yml`은 `npm run build` 후 `npm run smoke:full:prebuilt`를 실행한다.
- 2026-03-26: 공개 facade thin helper 추가. `src/core/Game.js`는 `createGame()` helper를, `src/scenes/play/PlayResultHandler.js`는 `createPlayResultHandler()` / `processPlayResult()` helper를 함께 노출한다. 기존 class import 경로는 유지했다.
- 2026-03-26: fresh 검증 재완료. `npm run build` PASS, `npm run smoke:full:prebuilt` `allPassed: true`, fresh `npm run verify:ci` PASS. 최종 `verify:ci`는 `typecheck`, `profile:check`, `lint:eslint`, `check:boundaries`, `check:architecture-docs`, 테스트 102/102, 단일 build, `smoke:core:prebuilt`까지 모두 통과했다.
- 2026-03-26: wrapper inventory 자동 판정 추가. `scripts/compatibilityWrappers.mjs`를 도입해 `src/tests/scripts` 기준 wrapper caller를 정적 import + dynamic import까지 집계하고, `docs/compatibility-wrappers.md`에 generated usage/caller snapshot을 기록하도록 했다. `check:architecture-docs`는 이제 이 generated section drift도 검사한다.
- 2026-03-26: wrapper repo 내부 caller 판정 결과. `playerSpawnRuntime`, `playSceneFlow`, `levelUpFlowRuntime`, event adapter wrapper들은 repo 내부 caller 0이다. 반면 `Game.js`는 `src/scenes/CodexScene.js` JSDoc/type import와 일부 테스트 때문에 3 caller, `PlayResultHandler.js`는 테스트 2 caller, `state/createWorld.js`는 source test 1 caller, `state/startLoadoutRuntime.js`는 테스트 2 caller가 남아 있어 즉시 삭제 대상은 아니다.
- 2026-03-26: 규범 문서 보강. `AGENTS.md`에 `lint:eslint` edit-time guard + `check:boundaries`/`check:architecture-docs` verify-time guard 이중 체계를 명시했다. `README.md`, `docs/architecture-current.md`, `docs/compatibility-wrappers.md`도 `compatibility:wrappers` 운영 경로를 반영했다.
- 2026-03-26: fresh 검증 재완료. `npm run lint` PASS, fresh `npm run verify:ci` PASS. 최종 `verify:ci`는 `typecheck`, `profile:check`, `lint`, 테스트 102/102, `vite build`, `smoke:core:prebuilt`까지 모두 통과했다.
- 2026-03-26: 시작 무기 선택 화면에 ESC 취소 경로를 추가했다. `StartLoadoutView`가 표시 중일 때 `Escape` 입력을 받아 취소 버튼과 동일한 `hide() + onCancel()` 흐름으로 타이틀 화면으로 복귀한다. `tests/TitleSceneContracts.test.js`에 회귀 테스트를 추가했고, 이후 fresh `npm run verify:ci`로 전체 기준선 재검증을 수행했다.
- 2026-03-26: 레벨업 카드 관계 태그 false positive를 수정했다. `levelUpFlowService.hasEvolutionRelation()`과 `synergyRuntime.hasSynergyRelation()`이 이제 “선택 카드 + 현재 보유 중인 나머지 재료로 실제 조건이 모두 충족되는 경우”에만 `진화 연관` / `시너지 연관` 힌트를 붙인다. `tests/ProgressionRuntime.test.js`에 회귀 테스트를 추가했고, 이후 fresh `npm run verify:ci`로 전체 기준선 재검증을 수행했다.
- 2026-03-26: 레벨업 카드 정보를 강화했다. `weapon_upgrade`/`accessory_upgrade` 카드는 이제 `Lv N → Lv N+1` 진행 라인을 표시하고, 도감에 아직 등록되지 않은 `weapon_new`/`accessory`/`weapon_evolution` 카드는 `도감 신규` 칩을 노출한다. `PlayScene`은 level-up overlay 데코레이션에 `session`을 함께 넘기고, `LevelUpView`는 상단 메타 스트립과 더 넓은 카드 레이아웃으로 갱신됐다. `tests/ProgressionRuntime.test.js`, `tests/LevelUpSource.test.js`에 회귀 테스트를 추가했고, 이후 fresh `npm run verify:ci`로 전체 기준선 재검증을 수행했다.
- 2026-03-26: 레벨업 카드 2차 개선. 강화 카드에 `다음 Lv 효과` 프리뷰를 추가했고, 도감 발견 판정을 `src/domain/meta/codex/codexDiscoveryDomain.js` 공용 helper로 승격해 Codex와 레벨업 카드가 같은 기준을 사용하게 했다. debug runtime에 `openLevelUpOverlay()`를 추가하고 deterministic smoke에 `levelup_overlay` 시나리오를 연결해 `Lv N → Lv N+1`, `다음 Lv 효과`, `도감 신규`, false positive 관계 태그 부재를 함께 검증한다.
- 2026-03-26: 레벨업 카드 3차 개선. 강화 카드에 `현재 효과`와 `다음 Lv 효과`를 나란히 비교하는 블록을 추가했고, `PlayUI`의 overlay visible 판정을 수정해 생성 전/숨김 상태에서 false를 정확히 반환하도록 정리했다. debug runtime의 pause/levelup/result helper는 다른 overlay를 먼저 닫아 snapshot이 단일 활성 상태를 반영하도록 맞췄고, `levelup_overlay` smoke는 이제 `현재 효과` 텍스트와 `pauseVisible/resultVisible=false`까지 검증한다. level-up 카드 CSS에는 모바일에서 비교 블록과 카드 폭이 안정적으로 내려오는 반응형 규칙을 추가했다.
- 2026-03-27: 레벨업 카드 UI를 `요약 우선` 구조로 재설계. `levelUpFlowService`가 `summaryText`를 제공하고, `LevelUpView`는 `현재 효과 / 다음 Lv 효과` 비교 블록 대신 `이름 + 핵심 요약 + 레벨 메타 + 보조 칩` 구조로 렌더한다. 카드별 리롤 규칙은 유지하면서 리롤 버튼 우선순위를 낮췄다.
- 2026-03-27: 회귀 검증 갱신. `tests/LevelUpSource.test.js`, `tests/ProgressionRuntime.test.js`, `tests/BrowserSmokeSource.test.js`를 새 요약형 계약에 맞게 red-green으로 갱신했고, `output/web-game/deterministic-smoke-core/levelup-overlay/shot.png` 스크린샷 기준으로 비교 블록 제거와 요약 한 줄 중심 레이아웃을 확인했다.
- 2026-03-27: 브라우저 smoke `levelup_overlay` 시나리오도 새 계약으로 갱신. 이제 `Lv 1 → Lv 2`, `.card-summary`, `도감 신규`, 레거시 비교 블록 부재를 검증하며, fresh `runSmokeAgainstPreview --scenario levelup_overlay` 결과 core suite 전체 `allPassed: true`.
- 2026-03-27: 레벨업 카드 선택 신호 2차 강화. `levelUpFlowService`가 타입/정의 기반 compact `summaryText`와 `priorityHint`를 만들고, 카드 배지는 `신규/강화/장신구/진화` 같은 의미형 한글 라벨로 교체했다. 진화/시너지 연관은 chips로 유지하면서 카드 상단에 `진화 빌드 연결` / `시너지 빌드 연결` pill로 승격했다.
- 2026-03-27: debug level-up smoke 샘플도 relation-positive 상태로 조정. `runtimeHooks.openLevelUpOverlay()`가 `magic_bolt` + `up_magic_bolt` 보유 상태를 함께 주입해 `get_boomerang`과 `get_tome_of_power` 카드에서 priority hint가 실제로 렌더되도록 바꿨다.
- 2026-03-27: fresh focused verification 완료. `tests/RuntimeHooks.test.js`, `tests/LevelUpSource.test.js`, `tests/ProgressionRuntime.test.js`, `tests/BrowserSmokeSource.test.js` 모두 PASS. `scripts/browser-smoke/runSmokeAgainstPreview.mjs --scenario levelup_overlay`도 `hasPromotedRelationHint=true`, `relationHintsRemainVisible=true`, `allPassed: true`로 통과했고 최신 스크린샷은 `output/web-game/deterministic-smoke-core/levelup-overlay/shot.png`.
